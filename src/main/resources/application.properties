spring.application.name=grpc-client-bff
server.port=8080

# gRPC channel → grpc-server
spring.grpc.client.channel.studentService.target=static://localhost:9090

# CORS for Angular dev server
app.cors.allowed-origins=http://localhost:4200
